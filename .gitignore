# Secrets
.env
.env.local
.env.test
.env.*.local

# Install artifacts
node_modules/
package-lock.json
bun.lock

# Daemon runtime artifacts — state lives outside the source tree
~/.accint/
/.accint/

# Subagent worktrees (per-agent git roots; never tracked)
.claude/worktrees/

# Orchestrator scheduling lock — local per-checkout state, not source
.claude/scheduled_tasks.lock

# Brain-bypass recovery quarantine — evidence of bypass mutations, kept
# locally for forensic review; not source under version control
.ralph/

# Editor / OS junk
.DS_Store
*.swp
*.swo

# Build output / scratch
/.dist/
/logs/

# Telegram MCP download artifacts (external tool output, not source)
/downloads/
